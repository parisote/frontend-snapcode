import React from 'react'
import { Routes, Route } from 'react-router-dom';

const Main = () => {

    return (
        <Routes>
            {/* <Route path='/' element={<Login />} /> */}
        </Routes>
    );
}
export default Main;